Need to implement keycloak and krakenD authentication
 1. Consider that krakenD will use OAuth2.0 strategy
 2. I will configure on the UI keycloak roles and permissions.
 3. Delete auth slice before implement new one
 4. Update docker-compose to be compatible with the new authentication and RBAC
 5. after all change migrations folder.